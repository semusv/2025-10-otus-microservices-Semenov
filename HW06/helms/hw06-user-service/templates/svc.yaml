apiVersion: v1
kind: Service
metadata:
  name: {{ include "hw06-user-service.fullname" . }}
  labels:
    {{- include "hw06-user-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      name: http
  selector:
    {{- include "hw06-user-service.selectorLabels" . | nindent 4 }}

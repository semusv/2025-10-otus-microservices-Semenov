{{- if .Values.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "hw07-api-gateway.fullname" . }}-metrics
  labels:
    {{- include "hw07-api-gateway.labels" . | nindent 4 }}
    app.kubernetes.io/component: metrics
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      name: http-metrics
  selector:
    {{- include "hw07-api-gateway.selectorLabels" . | nindent 4 }}
{{- end }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "hw07-user-service.fullname" . }}
  labels:
    {{- include "hw07-user-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      name: http
  selector:
    {{- include "hw07-user-service.selectorLabels" . | nindent 4 }}

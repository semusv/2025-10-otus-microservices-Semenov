env:
  db:
    - name: DB_HOST
      value: "hw07-postgresql"

    - name: DB_USER
      value: "app_user"

    - name: DB_NAME
      value: "microservices_db"

    - name: DB_PASSWORD
      valueFrom:
        secretKeyRef:
          name: postgresql-secret
          key: postgres-password
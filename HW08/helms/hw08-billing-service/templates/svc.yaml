apiVersion: v1
kind: Service
metadata:
  name: {{ include "hw08-billing-service.fullname" . }}
  labels:
    {{- include "hw08-billing-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      name: http
  selector:
    {{- include "hw08-billing-service.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "hw08-order-service.fullname" . }}
  labels:
    {{- include "hw08-order-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      name: http
  selector:
    {{- include "hw08-order-service.selectorLabels" . | nindent 4 }}

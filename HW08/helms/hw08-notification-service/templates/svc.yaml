apiVersion: v1
kind: Service
metadata:
  name: {{ include "hw08-notification-service.fullname" . }}
  labels:
    {{- include "hw08-notification-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      name: http
  selector:
    {{- include "hw08-notification-service.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "hw09-order-service.fullname" . }}
  labels:
    {{- include "hw09-order-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      name: http
  selector:
    {{- include "hw09-order-service.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "hw09-auth-service.fullname" . }}
  labels:
    {{- include "hw09-auth-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      name: http
  selector:
    {{- include "hw09-auth-service.selectorLabels" . | nindent 4 }}

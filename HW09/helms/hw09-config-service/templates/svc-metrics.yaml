{{- if .Values.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "hw09-config-service.fullname" . }}-metrics
  labels:
    {{- include "hw09-config-service.labels" . | nindent 4 }}
    app.kubernetes.io/component: metrics
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      name: http-metrics
  selector:
    {{- include "hw09-config-service.selectorLabels" . | nindent 4 }}
{{- end }}